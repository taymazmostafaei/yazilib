<% lyrics.forEach((lyric, index) => { %>
<% if (index % 2) { %>
📙 <%= lyric.title || '' %>
<%} else { %>
📘 <%= lyric.title || '' %>
<% } %>
📖 /g_<%= search.encode(lyric._id)  || '' %>
---------------------<% }) %>